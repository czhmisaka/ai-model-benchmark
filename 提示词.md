# 知识图谱构建系统 - 项目需求文档

## 一、项目整体目标

通过对话的方式构建知识图谱，系统包含以下核心功能：

1. **前端展示知识图谱** - 可视化交互界面
2. **数据持久化** - 后端 SQLite 数据库存储
3. **AI 辅助构建** - 对接 OpenAI 格式的大语言模型，通过多轮对话辅助用户编辑知识图谱
4. **版本控制** - 记录每一次变更并支持回退

---

## 二、前端技术栈

### 2.1 核心技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.5.24 | 前端核心框架 |
| Vue Router | ^4.6.3 | 路由管理 |
| Vite | ^7.2.4 | 构建工具 |
| axios | ^1.13.2 | HTTP 请求 |
| @vitejs/plugin-vue | ^6.0.1 | Vite Vue 插件 |

### 2.2 知识图谱可视化库

| 库 | 版本 | 用途 |
|----|------|------|
| D3.js | ^7.9.0 | 知识图谱的核心渲染与交互引擎 |

### 2.3 D3.js 详细说明

`D3.js` 在 GraphPanel.vue 组件中承担了图谱可视化的核心功能：

#### (1) 力导向图布局
- 使用 `d3.forceSimulation` 实现节点的物理仿真

#### (2) SVG 图形渲染
- 通过 SVG 绘制节点、边和标签

#### (3) 交互功能
- 缩放 (`d3.zoom`)
- 拖拽 (`d3.drag`)
- 节点/边的点击选中
- 鼠标悬停高亮

#### (4) 曲线边处理
- 支持多条边时的曲线分布
- 自环（self-loop）显示

#### (5) 动态效果
- 节点碰撞检测
- 力导向动画

---

## 三、后端技术要求

### 3.1 核心功能

1. **知识图谱的检索、编辑的原子化功能** - 提供细粒度的 API 接口
2. **变更记录** - 记录每一次变更的历史
3. **版本回退** - 支持回退到任意历史版本

### 3.2 数据存储

- 使用 **SQLite** 数据库持久化存储知识图谱数据

### 3.3 AI 对接

- 后端需要能对接 **OpenAI 格式** 的大语言模型
- 用户通过对话的方式，让模型通过多轮的思考和操作来编辑知识图谱

---

## 四、样式设计参考

### 4.1 字体系统

| 字体 | 用途 | 特性 |
|------|------|------|
| JetBrains Mono | 代码、标签、数字、徽章 | 等宽字体，强调技术感 |
| Space Grotesk | 主标题、导航 | 现代无衬线，几何感 |
| Noto Sans SC | 中文内容 | 清晰易读 |

### 4.2 配色方案

| 颜色 | 用途 | 色值 |
|------|------|------|
| Orange | 主强调色、标签、按钮 | #FF4500 |
| Black | 标题、导航栏 | #000000 |
| Gray | 次要文字、边框 | #666, #999 |
| White | 背景 | #FFFFFF |

### 4.3 布局风格

- **双栏布局** - 左右分屏，支持展开/折叠切换
- **响应式设计** - 支持 1024px 断点适配
- **面板化** - 卡片式组件，圆角 8px-10px
- **Flexbox** - 弹性盒模型主导

### 4.4 UI 元素特征

| 元素 | 样式关键词 |
|------|------------|
| 按钮 | 黑色背景 + 橙色悬停 + 边框脉冲动画 |
| 输入框 | 虚线边框 + 浅灰背景 + 代码字体 |
| 标签 | 橙色背景 + 圆角药丸形状 |
| 状态指示 | 圆点脉冲动画 (processing) |
| 面板 | 白色背景 + 浅灰边框 + 阴影 |

### 4.5 动效设计

- **过渡动画** - `cubic-bezier(0.25, 0.8, 0.25, 1)` 平滑过渡
- **脉冲动画** - 状态点呼吸效果
- **悬停反馈** - 颜色变化 + 位移 + 阴影
- **加载状态** - 旋转 spinner

### 4.6 图标风格

- **简约线条** - SVG stroke 描边风格
- **Unicode 符号** - 使用特殊字符 (■ ◇ ↻ ↑ ↓ →)
- **图标化标签** - 数字编号 + 描述

### 4.7 设计语言

> **技术感 + 现代感 + 极简主义**

- 强调色：橙色系
- 背景：大量留白 + 浅灰网格
- 交互：即时反馈
- 品牌字体：JetBrains Mono + Space Grotesk

### 4.8 Logo

> **Czhmisaka**

---

## 五、技术选型总结

```
┌─────────────────────────────────────────────────────────────┐
│                      项目架构                                 │
├─────────────────────────────────────────────────────────────┤
│  前端 (Vue 3 + Vite + D3.js)                                │
│    ├── 知识图谱可视化 (D3.js 力导向图)                        │
│    ├── 双栏交互界面                                         │
│    └── 现代化 UI 设计                                        │
├─────────────────────────────────────────────────────────────┤
│  后端 (Python + SQLite + OpenAI API)                        │
│    ├── 知识图谱原子化 API                                    │
│    ├── 变更历史记录                                         │
│    ├── 版本回退功能                                          │
│    └── LLM 对接 (OpenAI 格式)                               │
└─────────────────────────────────────────────────────────────┘