# 模型测试报告导出系统

## 功能概述

本系统为模型速度测试提供了完整的报告导出功能，支持多种格式：

### 支持的导出格式

1. **PDF 报告** - 专业的 PDF 文档格式
2. **Excel 表格** - 便于数据分析的 Excel 格式
3. **Markdown** - 纯文本格式，易于分享和版本控制
4. **HTML** - 网页格式，可在浏览器中查看

## 使用方法

### 前端使用

1. 进入"测试历史"页面
2. 点击任意测试记录的"详情"按钮
3. 在详情面板中可以看到以下按钮：
   - 📊 预览报告 - 弹出报告预览窗口
   - 📄 PDF - 下载 PDF 格式报告
   - 📝 Markdown - 下载 Markdown 格式报告
   - 📊 Excel - 下载 Excel 格式报告

### API 接口

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/history/{group_id}/report/pdf` | GET | 生成并下载 PDF 报告 |
| `/api/history/{group_id}/report/excel` | GET | 生成并下载 Excel 报告 |
| `/api/history/{group_id}/report/markdown` | GET | 获取 Markdown 报告内容 |
| `/api/history/{group_id}/report/html` | GET | 获取 HTML 报告内容 |

### 示例

```bash
# 下载 PDF 报告
curl http://localhost:15010/api/history/{group_id}/report/pdf -o report.pdf

# 下载 Excel 报告
curl http://localhost:15010/api/history/{group_id}/report/excel -o report.xlsx

# 获取 Markdown 报告
curl http://localhost:15010/api/history/{group_id}/report/markdown
```

## 技术实现

### 后端技术

- **Python 3.8+**
- **ReportLab** - PDF 生成
- **OpenPyXL** - Excel 导出
- **Markdown** - Markdown 到 HTML 转换

### 前端技术

- **Vue 3 + TypeScript**
- **ECharts** - 图表可视化
- **Marked** - Markdown 解析

## 文件结构

```
model_speed_test/
├── web/
│   ├── app.py                    # API 端点
│   ├── report_generator.py        # Markdown/PDF/HTML 报告生成器
│   └── excel_exporter.py         # Excel 导出器
└── frontend/
    └── src/
        ├── components/
        │   └── ReportPreviewModal.vue  # 报告预览弹窗
        └── views/
            └── History.vue         # 历史页面（包含导出按钮）
```

## 报告内容

生成的报告包含：

### 概览统计
- 测试组 ID
- 测试时间
- 总测试数
- 成功/失败数量
- 成功率

### 性能指标
- 平均 TTFT（首 Token 时间）
- 平均 TPS（Token 生成速度）
- 平均输出 Token 数

### 模型对比
- 各模型的表现对比
- 性能排名

### 详细结果
- 每轮测试的详细数据
- 时间戳
- 错误信息（如有）

## 依赖安装

### 后端依赖

```bash
pip install reportlab openpyxl markdown
```

### 前端依赖

```bash
cd frontend
npm install marked
```

## 注意事项

1. **数据库连接**：报告生成需要连接到数据库，确保 `results/test_results.db` 存在
2. **权限问题**：确保对 `results/` 目录有写入权限
3. **网络问题**：如果使用 PDF 生成失败，检查是否安装了 `reportlab`

## 故障排除

### 问题：PDF 导出失败
**解决方案**：
```bash
pip install reportlab
```

### 问题：Excel 导出失败
**解决方案**：
```bash
pip install openpyxl
```

### 问题：Markdown 解析失败
**解决方案**：
```bash
npm install marked
```

## 未来计划

- [ ] 支持自定义报告模板
- [ ] 添加更多图表类型
- [ ] 支持中文语言包
- [ ] 添加批量导出功能
- [ ] 支持邮件发送报告

## 开发者信息

- 开发时间：2026-03-30
- 开发者：CZH
- 版本：1.0.0
